# Meta Skill Studio Tasks

This directory is for human-readable worklogs and generated Studio task artifacts.

`tasks/pipelines/` is created by `ingest-skill-fault`, `run-pipeline`, and `resume-pipeline`. The generated pipeline state, report, and log files are runtime evidence and are intentionally ignored by git.
